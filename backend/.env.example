# OpenAI API key (optional — heuristic fallback runs without it)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Model for classification (structured JSON)
OPENAI_MODEL=gpt-4o-mini
GEMINI_MODEL=gemini-3.6-flash

# Port the Express API listens on
PORT=3001